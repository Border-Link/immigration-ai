"""
Test suite for the `immigration_cases` Django app.

Production standards:
- Use services (not models) as the entrypoint for domain operations.
- Exercise both service layer and API layer behavior.
"""

